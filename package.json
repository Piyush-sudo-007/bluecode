{
  "name": "bluecode",
  "version": "1.0.0",
  "main": "./Backend/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon Backend/index.js",
    "build": "npm install && cd Jarvis && apt-get update && apt-get install -y espeak-ng portaudio19-dev gcc && pip install -r requirements.txt && cd ../Frontend && npm install && npm run build"
  },
  "author": "piyush",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "child_process": "^1.0.2",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
